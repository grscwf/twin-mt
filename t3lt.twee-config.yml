sugarcube-2:
  macros:
    add-action:
      name: add-action
    announce-barbs:
      name: announce-barbs
      parameters: ["'now'|'soon'|'maybe'"]
    assert-fail:
      name: assert-fail
    assert-true:
      name: assert-true
    arousalbar:
      name: arousalbar
    big-mood:
      name: big-mood
      parameters:
        - "(bareword|var) &+ ( 'ignore'|'TODO'|'vary' )"
        - "(bareword|var) &+ ( 'always'|'assert'|'implies' ) &+ ...text"
    delete-vars:
      name: delete-vars
      parameters: ['bareword |+ ...bareword']
    fade-in:
      name: fade-in
      children: ['next']
      container: true
      parameters: ['|+ bareword']
    get-actions:
      name: get-actions
      parameters: ['']
    n1-patience:
      name: n1-patience
      parameters: ['number &+ number &+ text |+ text |+ text']
    no-trace:
      name: no-trace
      container: true
    optional-action:
      name: optional-action
      parameters: ['text']
    pw-another:
      name: pw-another
      parameters: ['text |+ text']
    pw-choice:
      name: pw-choice
      children: ['pw-another', 'pw-fail']
      container: true
      parameters: ['text |+ text']
    pw-fail:
      name: pw-fail
    redisplay-passage:
      name: redisplay-passage
    timed-beat:
      name: timed-beat
      container: true
      parameters: ['text |+ text']
    "TOD\x4f":
      name: "TOD\x4f"

