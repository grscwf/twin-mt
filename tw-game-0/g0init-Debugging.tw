:: g0init Debugging [inclusion] {"position":"740,450","size":"100,100"}
<<script>>

function debuggingInit() {
  if (!setup.debug) return;

  $(document).on(":passagedisplay", () => {
    // SugarCube debug mode puts the whole passage source in
    // a title hover text, which is useless and distracting
    $(`[title^="<\<nobr>>"], [title^="<\<script>>"]`)
      .removeAttr("title")
      .removeAttr("aria-label");
  });
}

debuggingInit();

<</script>>
